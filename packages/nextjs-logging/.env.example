# NextJS Logging Package - Environment Variables
#
# This file documents all environment variables used by the @core-mfg/nextjs-logging package.
# Copy this file to .env.local in your project and configure the values as needed.

# ==========================================
# STORAGE CONFIGURATION
# ==========================================

# Storage backend type for logging configuration persistence
# Options: 'memory' (default), 'file', 'redis'
# Note: 'file' storage is not available in browser/client environments
LOGGING_STORAGE_TYPE=memory

# Configuration file path (only used when LOGGING_STORAGE_TYPE=file)
# This is the path where logging configuration will be persisted on disk
LOGGING_CONFIG_FILE=config/logging-config.json

# Redis connection URL (only used when LOGGING_STORAGE_TYPE=redis)
# Format: redis://[username:password@]host:port[/database]
# Example: redis://user:pass@redis-host:6379/0
REDIS_URL=redis://localhost:6379

# ==========================================
# RUNTIME ENVIRONMENT
# ==========================================

# Node.js environment (automatically set by Next.js)
# Used to set default log levels: 'debug' in development, 'info' in production
# Values: 'development', 'production', 'test'
NODE_ENV=development

# ==========================================
# EXAMPLES
# ==========================================

# Development setup with file storage:
# LOGGING_STORAGE_TYPE=file
# LOGGING_CONFIG_FILE=config/logging-config.json

# Production setup with Redis:
# LOGGING_STORAGE_TYPE=redis
# REDIS_URL=redis://user:password@redis-host:6379/0

# Minimal setup (in-memory storage):
# LOGGING_STORAGE_TYPE=memory